# SwapApp - Item Trading Platform

A modern web application for trading items between users, built with Laravel 11 and Vue 3.

## 🚀 Features

- **User Authentication**: Secure registration and login with Laravel Sanctum token-based authentication
- **Item Management**: Add, edit, delete, and view items with images
- **Image Upload**: Upload and display product images
- **Trade Proposals**: Propose trades between items
- **Multilingual Support**: Items support multiple languages (English)
- **Responsive Design**: Mobile-friendly interface with modern UI
- **RESTful API**: Clean API architecture with proper authentication middleware

## 🛠️ Tech Stack

### Backend

- **Laravel 11**: PHP framework with modern features
- **MySQL**: Database for data persistence
- **Laravel Sanctum**: API authentication
- **Eloquent ORM**: Database relationships and queries

### Frontend

- **Vue 3**: Progressive JavaScript framework with Composition API
- **Vite**: Fast build tool with HMR (Hot Module Replacement)
- **Vue Router**: Client-side routing with auth guards
- **Pinia**: State management for user authentication
- **Axios**: HTTP client for API requests
- **SCSS**: Styling with preprocessor

## 📋 Requirements

- PHP 8.2 or higher
- Composer
- Node.js 18+ and npm
- MySQL 5.7+ or MariaDB
- Git

## 📦 Installation

### Quick Setup (Windows)

Run the automated installer:

```bash
install.bat
```

### Manual Setup

#### 1. Clone the Repository

```bash
git clone <repository-url>
cd swapapp
```

#### 2. Backend Setup

```bash
# Navigate to backend directory
cd backend

# Install PHP dependencies
composer install

# Copy environment file
copy .env.example .env

# Generate application key
php artisan key:generate

# Configure database in .env file
# DB_DATABASE=gp_db
# DB_USERNAME=root
# DB_PASSWORD=

# Run migrations
php artisan migrate

# Create storage symlink
php artisan storage:link

# Start Laravel server
php artisan serve
```

Backend will run on: `http://127.0.0.1:8000`

#### 3. Frontend Setup

```bash
# Navigate to frontend directory
cd frontend

# Install Node dependencies
npm install

# Copy environment file (if needed)
copy .env.example .env

# Start development server
npm run dev
```

Frontend will run on: `http://localhost:5173`

## ⚙️ Configuration

### Backend (.env)

```env
APP_NAME=SwapApp
APP_ENV=local
APP_KEY=base64:... # Generated by php artisan key:generate
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=gp_db
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SANCTUM_STATEFUL_DOMAINS=localhost:5173
```

### Frontend (src/utils/api.js)

```javascript
baseURL: "http://localhost:8000/api";
```

## 📁 Project Structure

```
swapapp/
├── backend/                    # Laravel 11 API
│   ├── app/
│   │   ├── Http/
│   │   │   ├── Controllers/
│   │   │   │   └── Api/       # API Controllers
│   │   │   └── Middleware/    # CORS & Auth middleware
│   │   └── Models/            # Eloquent models
│   ├── config/                # Configuration files
│   ├── database/
│   │   └── migrations/        # Database migrations
│   ├── routes/
│   │   └── api.php           # API routes
│   ├── storage/
│   │   └── app/public/items/ # Uploaded images
│   └── public/               # Public assets
│
├── frontend/                  # Vue 3 SPA
│   ├── src/
│   │   ├── assets/           # Static assets & styles
│   │   ├── components/       # Reusable components
│   │   ├── router/           # Vue Router config
│   │   ├── stores/           # Pinia stores
│   │   ├── utils/            # API client & helpers
│   │   ├── views/            # Page components
│   │   ├── App.vue           # Root component
│   │   └── main.js           # Application entry
│   └── public/               # Static files
│
├── .gitignore               # Git ignore rules
├── README.md                # This file
└── install.bat              # Windows installer
```

## 🎯 Usage

### User Registration

1. Navigate to `http://localhost:5173/register`
2. Fill in username, email, password
3. Submit to create account

### Add Items

1. Login to your account
2. Click "Add Product"
3. Fill in title, description, price
4. Upload an image (under 8MB)
5. Submit to create item

### View Items

- Browse all items at `/items`
- View your items at `/account`

### Trade Items

1. Browse items
2. Click "Propose Trade" on desired item
3. Select your item to trade
4. Submit proposal

## 🔧 Development

### Backend Development

```bash
cd backend

# Run migrations
php artisan migrate

# Fresh migrations (drops all tables)
php artisan migrate:fresh

# Clear cache
php artisan cache:clear
php artisan config:clear

# Run tests (when implemented)
php artisan test
```

### Frontend Development

```bash
cd frontend

# Development server with HMR
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview

# Lint code
npm run lint
```

## 📝 API Documentation

### Authentication Endpoints

- `POST /api/register` - Register new user
- `POST /api/login` - Login user
- `POST /api/logout` - Logout user (requires auth)
- `GET /api/user` - Get authenticated user (requires auth)

### Item Endpoints (Requires Authentication)

- `GET /api/items` - List all items
- `POST /api/items` - Create new item
- `GET /api/items/{id}` - Get item details
- `PUT /api/items/{id}` - Update item
- `DELETE /api/items/{id}` - Delete item
- `GET /api/my-items` - Get current user's items

### Trade Endpoints (Requires Authentication)

- `GET /api/trades` - List user's trades
- `POST /api/trades` - Create trade proposal

## 🔒 Security

- Passwords hashed with bcrypt
- CSRF protection on all state-changing requests
- Token-based API authentication with Sanctum
- Input validation on all endpoints
- SQL injection protection via Eloquent ORM
- XSS protection via Vue's template sanitization

## 📄 License

This project is proprietary software. All rights reserved.

## 👥 Contributors

- **Development Team**: Jackcubbi - Anatoli Rostsin
